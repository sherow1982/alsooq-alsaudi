# 🛠️ دليل المطور - السوق السعودي

## نظرة عامة على المشروع

موقع تجارة إلكترونية متكامل يحتوي على 2188 منتج مع تحسينات SEO كاملة ومتوافق مع Google Merchant Center.

---

## 📁 هيكل المشروع

```
alsooq-alsaudi/
├── products/              # 2228 صفحة منتج HTML
├── css/                   # ملفات التنسيق
├── scripts/               # سكريبتات JavaScript
├── *.py                   # سكريبتات Python
├── product-feed.xml       # ملف المنتجات لـ GMC
├── sitemap.xml           # خريطة الموقع
├── products.json         # بيانات المنتجات
├── descriptions.json     # أوصاف المنتجات
└── *.md                  # ملفات التوثيق
```

---

## 🐍 السكريبتات المتاحة

### سكريبتات الإصلاح:
| السكريبت | الوظيفة | الاستخدام |
|---------|---------|----------|
| `fix_all.py` | تشغيل جميع الإصلاحات | `python fix_all.py` |
| `fix_feed_gmc.py` | إصلاح ملف المنتجات XML | `python fix_feed_gmc.py` |
| `fix_products.py` | إصلاح مسارات CSS | `python fix_products.py` |
| `fix_schema.py` | إصلاح Schema.org | `python fix_schema.py` |
| `fix_html_encoding.py` | إصلاح ترميز HTML | `python fix_html_encoding.py` |

### سكريبتات التوليد:
| السكريبت | الوظيفة | الاستخدام |
|---------|---------|----------|
| `generate_all_pages.py` | توليد صفحات المنتجات | `python generate_all_pages.py` |
| `generate_sitemap.py` | توليد خريطة الموقع | `python generate_sitemap.py` |
| `generate_missing_products.py` | توليد المنتجات المفقودة | `python generate_missing_products.py` |

### سكريبتات التحديث:
| السكريبت | الوظيفة | الاستخدام |
|---------|---------|----------|
| `update_product_feed.py` | تحديث ملف المنتجات | `python update_product_feed.py` |
| `update_sitemap.py` | تحديث خريطة الموقع | `python update_sitemap.py` |
| `seo_optimizer.py` | تحسين SEO | `python seo_optimizer.py` |

### سكريبتات الصيانة:
| السكريبت | الوظيفة | الاستخدام |
|---------|---------|----------|
| `run_all_maintenance.py` | صيانة شاملة | `python run_all_maintenance.py` |
| `check_status.py` | فحص الحالة | `python check_status.py` |

---

## 🚀 البدء السريع

### 1. المتطلبات:
```bash
pip install -r requirements.txt
```

### 2. التشغيل الأول:
```bash
python fix_all.py
```

### 3. التحقق من النتائج:
```bash
python check_status.py
```

---

## 🔧 التطوير

### إضافة منتجات جديدة:

1. أضف المنتجات إلى `products.json`
2. أضف الأوصاف إلى `descriptions.json`
3. شغل:
```bash
python fix_all.py
```

### تحديث صفحة منتج:

1. عدل البيانات في `products.json`
2. شغل:
```bash
python generate_all_pages.py
python seo_optimizer.py
```

### تحديث التصميم:

1. عدل ملفات CSS في مجلد `css/`
2. لا حاجة لتشغيل سكريبتات

---

## 📊 الإحصائيات

```python
# الحصول على إحصائيات المشروع
python check_status.py
```

النتيجة المتوقعة:
```
Products in JSON: 2188
Product HTML files: 2228
[OK] index.html exists
[OK] about.html exists
[OK] contact.html exists
[OK] sitemap.xml exists
[OK] product-feed.xml exists
```

---

## 🧪 الاختبار

### اختبار محلي:

```bash
# تشغيل خادم محلي
python -m http.server 8000

# افتح المتصفح
# http://localhost:8000
```

### اختبار الروابط:

```bash
# فحص جميع الروابط
python check_status.py
```

---

## 🐛 استكشاف الأخطاء

### خطأ في الترميز UTF-8:
```python
# جميع السكريبتات تحتوي على:
if sys.platform == 'win32':
    sys.stdout.reconfigure(encoding='utf-8')
```

### خطأ في المسارات:
```bash
# تأكد من تشغيل السكريبتات من المجلد الرئيسي
cd c:\Users\sherow\Desktop\alsooq-alsaudi
python fix_all.py
```

### خطأ في استيراد المكتبات:
```bash
pip install -r requirements.txt
```

---

## 📝 ملفات التوثيق

| الملف | المحتوى |
|------|---------|
| `README.md` | معلومات الشركة |
| `DEVELOPER_README.md` | هذا الملف |
| `FIXES_REPORT.md` | تقرير الإصلاحات |
| `QUICK_GUIDE.md` | دليل سريع |
| `SUMMARY.md` | ملخص شامل |
| `DEPLOYMENT_GUIDE.md` | دليل النشر |

---

## 🔐 الأمان

- ✅ لا توجد بيانات حساسة في الكود
- ✅ جميع الروابط آمنة (HTTPS)
- ✅ لا توجد ثغرات XSS
- ✅ التحقق من المدخلات

---

## 🌐 التوافق

### المتصفحات:
- ✅ Chrome/Edge (آخر إصدار)
- ✅ Firefox (آخر إصدار)
- ✅ Safari (آخر إصدار)
- ✅ Mobile browsers

### Python:
- ✅ Python 3.8+
- ✅ Windows 10/11
- ✅ Linux/Mac

---

## 📈 الأداء

### سرعة التوليد:
- صفحات المنتجات: ~23 ثانية (2188 صفحة)
- تحسين SEO: ~67 ثانية (2188 صفحة)
- خريطة الموقع: ~2 ثانية

### التحسينات:
- ✅ معالجة متوازية (Parallel Processing)
- ✅ تخزين مؤقت (Caching)
- ✅ تحميل كسول (Lazy Loading)

---

## 🤝 المساهمة

### إضافة ميزة جديدة:

1. أنشئ فرع جديد:
```bash
git checkout -b feature/new-feature
```

2. اعمل التغييرات

3. اختبر:
```bash
python check_status.py
```

4. ارفع:
```bash
git add .
git commit -m "إضافة ميزة جديدة"
git push origin feature/new-feature
```

---

## 📞 الدعم التقني

للمساعدة التقنية:
- 📧 البريد: sherow1982@gmail.com
- 📱 الهاتف: +201110760081

---

## 📜 الترخيص

© 2024 السوق السعودي للتجارة الإلكترونية - جميع الحقوق محفوظة

---

## 🔄 آخر تحديث

**التاريخ**: يناير 2026  
**الإصدار**: 2.0  
**الحالة**: ✅ مستقر ومختبر

---

## 🎯 الخطط المستقبلية

- [ ] إضافة نظام بحث متقدم
- [ ] تحسين سرعة التحميل
- [ ] إضافة مرشحات المنتجات
- [ ] تحسين تجربة المستخدم على الموبايل
- [ ] إضافة نظام تقييمات
- [ ] دعم لغات إضافية

---

**ملاحظة**: هذا المشروع في تطور مستمر. تابع التحديثات!
