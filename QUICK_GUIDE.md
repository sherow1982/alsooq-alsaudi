# دليل الاستخدام السريع - السوق السعودي

## 🚀 تشغيل جميع الإصلاحات دفعة واحدة

```bash
python fix_all.py
```

هذا الأمر سيقوم بتشغيل جميع السكريبتات تلقائياً وإصلاح كل شيء!

---

## 📋 السكريبتات المتاحة

### 1. الإصلاحات الأساسية
```bash
python fix_feed_gmc.py          # إصلاح ملف المنتجات XML
python fix_products.py          # إصلاح مسارات CSS
python fix_schema.py            # إصلاح Schema.org
python fix_html_encoding.py     # إصلاح ترميز HTML
```

### 2. توليد المحتوى
```bash
python generate_all_pages.py   # توليد صفحات المنتجات
python generate_sitemap.py      # توليد خريطة الموقع
```

### 3. التحسينات
```bash
python seo_optimizer.py         # تحسين SEO والسكيما
python update_product_feed.py   # تحديث ملف المنتجات
```

### 4. الصيانة
```bash
python run_all_maintenance.py   # تشغيل الصيانة الشاملة
python check_status.py          # فحص حالة الموقع
```

---

## 🎯 الاستخدام الموصى به

### للتحديث اليومي:
```bash
python run_all_maintenance.py
```

### للإصلاح الشامل:
```bash
python fix_all.py
```

### للتحقق من الحالة:
```bash
python check_status.py
```

---

## 📊 معلومات الموقع

- **عدد المنتجات**: 2188
- **عدد الصفحات**: 2228
- **رابط الموقع**: https://sherow1982.github.io/alsooq-alsaudi/
- **ملف المنتجات**: https://sherow1982.github.io/alsooq-alsaudi/product-feed.xml
- **خريطة الموقع**: https://sherow1982.github.io/alsooq-alsaudi/sitemap.xml

---

## ⚡ نصائح سريعة

1. **قبل رفع التغييرات إلى GitHub**:
   ```bash
   python check_status.py
   ```

2. **بعد إضافة منتجات جديدة**:
   ```bash
   python fix_all.py
   ```

3. **للتحديث السريع**:
   ```bash
   python run_all_maintenance.py
   ```

---

## 🔧 حل المشاكل

### إذا ظهرت أخطاء في الترميز:
- تأكد من أن Python يستخدم UTF-8
- جميع السكريبتات تم إصلاحها للعمل على Windows

### إذا كانت الصفحات لا تظهر:
```bash
python generate_all_pages.py
python generate_sitemap.py
```

### إذا كان ملف المنتجات به مشاكل:
```bash
python fix_feed_gmc.py
```

---

## 📞 الدعم

للمزيد من المعلومات، راجع:
- `FIXES_REPORT.md` - تقرير الإصلاحات المفصل
- `README.md` - معلومات الشركة والمشروع

---

**ملاحظة**: جميع السكريبتات تعمل الآن بشكل صحيح على Windows مع دعم كامل للغة العربية!
